name: Terraform CI (Workflow Env)

on:
  push:
    branches: [ master ]

env:
  TF_WORKSPACE: production
  AWS_REGION: ap-south-1
  PROJECT_NAME: payments-api
  ENVIRONMENT: production

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - run: terraform init
      - run: terraform plan
